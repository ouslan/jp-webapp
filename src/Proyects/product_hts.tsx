import React from 'react';
import { useNavigate } from 'react-router-dom';

const product_hts: React.FC = () => {
  const navigate = useNavigate();

  return (
    <div>Product HTS</div>
  );
};

export default product_hts;
